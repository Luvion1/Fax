fn main() {
    let arr = [1, 2, 3];
    arr[1] = 10;
    print(arr[1]); // Harusnya 10
    
    // Nested array?
    // let matrix = [[1, 2], [3, 4]]; 
    // Fax mungkin belum mendukung nested array literal dengan benar di Codegen
}
