struct Node { val: i64 }

fn leak_checker(n: i64): i64 {
    let node = Node { val: n };
    if (n <= 0) {
        Std::io::collect_gc(); 
        return 0;
    }
    return node.val + leak_checker(n - 1);
}

fn main() {
    let result = leak_checker(10);
    print(result);
}