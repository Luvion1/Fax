struct Node { val: i64, next: Node }

fn main() {
    let mut head: Node = Node { val: 0, next: null };
    for i in 1..10 {
        head = Node { val: i, next: head };
    }
    
    collect_gc();
    print(head.val);
    print(head.next.val);
}
