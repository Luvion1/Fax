struct Point { x: i64, y: i64 }
fn main() {
    let mut pts: Point[] = [Point { x: 1, y: 2 }, Point { x: 3, y: 4 }];
    print(pts[0].x);
    pts[0].x = 100;
    print(pts[0].x);
    
    let mut p = pts[1];
    print(p.y);
    p.y = 200;
    print(p.y);
    print(pts[1].y);
}
