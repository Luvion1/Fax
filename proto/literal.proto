syntax = "proto3";

package fax.compiler;

import "common.proto";
import "types.proto";

// Literals
message Literal {
  oneof literal_kind {
    int64 int_val = 1;
    double float_val = 2;
    bool bool_val = 3;
    string string_val = 4;
    int32 char_val = 5;
  }
}
